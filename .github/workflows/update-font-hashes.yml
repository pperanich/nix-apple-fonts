name: Update font hashes

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  update-hashes:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/checkout@v5

      - uses: DeterminateSystems/determinate-nix-action@v3

      - run: determinate-nixd fix hashes --auto-apply

      - name: Push updated hashes and tag release
        run: |
          if ! git diff --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add --update --ignore-removal .
            git commit -m "fix: update font hashes"
            git push

            # Auto-increment patch version and push tag to trigger FlakeHub publish
            latest=$(git tag --sort=-v:refversion --list 'v*' | head -1)
            if [ -z "$latest" ]; then
              next="v0.1.0"
            else
              next=$(echo "$latest" | awk -F. '{print $1"."$2"."$3+1}')
            fi
            git tag "$next"
            git push origin "$next"
          fi
